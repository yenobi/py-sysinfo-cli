[project]
name = "sysinfo"
version = "0.1.0"
description = "Non-production linux system information CLI written in Python to practice language skills"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
sysinfo = "sysinfo:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
